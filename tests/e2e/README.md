# End-to-end tests

Reserved for Playwright browser tests (Chromium/Firefox/WebKit, spec
section 16) once Milestone 2/4 add real pages to drive: the enrollment/
waiting flow and the admin console. Nothing to test end-to-end yet --
every route this milestone declares is a stub (see each `internal/*`
package's `doc.go`).
