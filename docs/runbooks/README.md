# Runbooks

Reserved for operational runbooks (rollout/rollback, backup/restore,
key rotation, incident response) -- see spec section 13 and Milestone 5's
exit gate. Nothing to document yet: there is no deployable artifact
(`deploy/stack.yml`) until then.
